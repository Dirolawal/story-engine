ANTHROPIC_API_KEY=your_api_key_here

# Per-client passwords — one per client folder under clients/
# The password_env field in each clients/<id>/config.json points here
BASIT_PASSWORD=
ASLI_PASSWORD=
DEBORAH_PASSWORD=
